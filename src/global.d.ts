interface Window {
    HTTP_PORT: number;
    NODE_ADDRESS: string;
    BLOBS_PATH: string;
    GRAPHQL_ENDPOINT: string;
    LATEST_SPRITE: any;
    session: any; // Keep as 'any' for backward compatibility
}